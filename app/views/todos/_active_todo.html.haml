%ul.list-group
  %li.list-group-item
    %input{id: "box-#{active_todo.id}", type: "checkbox"}
    %label{for: "box-#{active_todo.id}"}
    %p
      = active_todo.title
    = link_to "削除", "/goals/#{active_todo.stage.goal.id}/stages/#{active_todo.stage.id}/todos/#{active_todo.id}", method: "delete"